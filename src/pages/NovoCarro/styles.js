// vitals
import styled from 'styled-components';

const NovoCarroStyled = styled.section`
  .title-container {
    alignt-items: space-between;
    display: flex;
  }
`;

export default NovoCarroStyled;
